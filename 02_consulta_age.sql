-- Seleciona da coluna age e já realiza a média através de avg e renomeia para idade_media
select

avg(age) as idade_media

-- Tabela onde está localizada no banco de dados.
from leads_basic_details